<ul class="list p13">
    {{#each model as |song|}}
    <li>
        {{song.title}} by <em>{{song.band}}</em><!-- accedo a las propiedades del modelo song -->
        <span class="rating fr">{{song.rating}}</span>
    </li>
    {{/each}}
</ul>